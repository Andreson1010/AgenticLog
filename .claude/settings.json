{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "git rev-parse --abbrev-ref HEAD | grep -qv '^main$' || { echo 'ERROR: commit direto na branch main bloqueado. Crie uma feature branch primeiro: git checkout -b feature/<nome>' >&2; exit 2; }",
            "if": "Bash(git commit*)"
          }
        ]
      },
      {
        "matcher": "PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "git rev-parse --abbrev-ref HEAD | grep -qv '^main$' || { echo 'ERROR: commit direto na branch main bloqueado. Crie uma feature branch primeiro: git checkout -b feature/<nome>' >&2; exit 2; }",
            "if": "PowerShell(git commit*)"
          }
        ]
      }
    ]
  }
}
